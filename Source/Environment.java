class Environment
{
    private String PlayerUpdate;
    inputhandler object=new inputhandler();
    
    void play()
    {
        while((((selectplayer && computesgoal) && computesthemove )&& fires))
        {
           //update player
        }
    }
}